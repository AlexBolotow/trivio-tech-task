# Задачи: импорт файлов поставщиков

- [ ] Рассмотреть и одобрить proposal.md, включая объём первого этапа и то, что в него не входит.
- [ ] Согласовать и утвердить ER-схему и блокирующие решения из design.md до создания миграций.
- [x] Первый участок — база и модели:
  - [x] Создать миграции в `database/migrations` для поставщиков/настроек, родительских и дочерних задач, текущих supplier hotels/room types, Expedia/Pegas extensions и фотографий.
  - [x] Добавить FK, unique constraints, индексы, nullable canonical links и правила удаления; проверить соответствие UUID каталога и BIGINT внутренних ID.
  - [x] Добавить идемпотентный seeder поставщиков с кодами `expedia` и `pegas`; не привязывать код к numeric IDs и не выдумывать незаданные настройки.
  - [x] Добавить Eloquent `Record` классы в `Import/Infrastructure/Persistence/Eloquent/Record` с casts и relations; не добавлять generic repository или Active Record бизнес-логику.
  - [x] Реализовать только обоснованные Domain-модели parent import и chunk, statuses/value types и разрешённые переходы; не создавать Domain-сущность на каждую таблицу.
  - [x] Реализовать специализированный mapper в persistence repository для round-trip Domain ↔ Eloquent; транзакцией управляет Application use case.
  - [x] Добавить unit tests на lifecycle/retry/threshold и MySQL integration tests на миграции, constraints, seeder и mapper.
- [ ] Провести ревью и зафиксировать результат этого участка перед реализацией обработчиков файлов.
- [x] Зафиксировать настройки поставщика: импорт включён/выключен, интервал проверки источника, размер дочерней задачи и минимальная доля успеха (по умолчанию 80%).
- [x] Зафиксировать переход задачи в `in_progress` при захвате воркером и перевод в `error` после пяти повторов сверх первой попытки.
- [x] Выбрать направление хранения: общие текущие записи поставщика обновляются на месте, специфичные поля вынесены в расширения; `last_seen_import_id` отмечает родительский импорт, обновивший запись.
- [ ] Согласовать ER-схему и детализацию полей поставщиков, исходных payload и критерия полноты импорта для обработки отсутствующих записей.
- [ ] Выбрать механизм выполнения задач: Laravel Queue с MySQL backend и `Bus::batch` либо собственные воркеры над таблицами задач.
- [ ] Согласовать, как требование heartbeat старше пяти минут сочетается с Laravel Queue `retry_after`, timeout и размером chunk.
- [ ] Выбрать, как хранить срок следующей проверки поставщика и предотвращать параллельную проверку одного источника.
- [x] Зафиксировать оценку родителя после завершения всех дочерних задач: доля `done` сравнивается с порогом поставщика.
- [ ] Определить, считается ли импорт достаточно полным для поиска отсутствующих гостиниц, если часть дочерних задач завершилась ошибкой, но достигнут порог 80%.
- [ ] Решить, как получать ограниченные chunks из однострочного XML и нужна ли параллельная обработка этого файла на первом этапе.
- [ ] Уточнить состояния родительской и дочерней задачи, частоту heartbeat, обработку устаревшего владения и политику ошибок записей/карантина.
- [ ] Определить, что делать с гостиницами/номерами, не встреченными в достаточно полном импорте (например, помечать неактивными или сохранять без изменения).
- [ ] Определить публичный контракт между Import, SupplierIntegration, Matching и Catalog.
- [x] Выполнить первый участок из этого плана; публичные контракты между Import, SupplierIntegration и Matching добавлять только вместе с первым use case, который в них нуждается.
- [ ] Реализовать один вертикальный поток импорта с выбранной Laravel Queue или собственной очередью, идемпотентной обработкой chunks и MySQL integration-тестами.
- [ ] Добавить readers обоих форматов поставщиков и тесты на синтетических NDJSON/XML-файлах.
- [ ] Проверить расписание и дедупликацию файла, захват задачи, heartbeat/timeout, пять повторов, порог успеха 80%, идемпотентный upsert, обновление `last_seen_import_id` и обработку отсутствующих записей.
- [ ] Сверить реализацию с одобренными spec/design и архивировать change.
